
# django-dict-form

The standard implementation of a form in the Django framework contains standard methods for displaying in templates, such as `as_div`, `as_table` and so on.
This project extends several methods for the class as `as_dict`, `as_json`.