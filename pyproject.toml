[project]
name = "django-json-form"
version = "0.1.0"
description = "battery for modification of class `forms.Form`, which displays dict and json"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
]
